#include "main.h"

/**
 * _strlen - Returns 
 * @str: ..
 * Return: 1
 */

int _strlen(char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)
		return (i);
}

/**
 * _strlenc - ..
 * @str: ..
 * Return: i
 */

int _strlenc(const char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)
		return (i);
}
